name: ot-defence-workbench

topology:
  nodes:
    north:
      kind: bridge

    south:
      kind: bridge

    client:
      kind: linux
      image: ot-workbench/client:latest
      network-mode: none

    probe:
      kind: linux
      image: ot-workbench/probe:latest
      network-mode: none

    asset:
      kind: linux
      image: ot-workbench/asset:latest
      network-mode: none

    boundary:
      kind: linux
      image: ot-workbench/boundary:latest
      network-mode: none

  links:
    - endpoints: ["client:eth1", "north:veth-client"]
    - endpoints: ["probe:eth1", "north:veth-probe"]
    - endpoints: ["boundary:eth1", "north:veth-bnd-n"]
    - endpoints: ["boundary:eth2", "south:veth-bnd-s"]
    - endpoints: ["asset:eth1", "south:veth-asset"]